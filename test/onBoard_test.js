var assert = require('assert');
var OnBoard = require("../onBoard");

describe('Test How Many On Board', function(){
	it("should find Numbers of cars and people on board", function(Done){
      var ferry = new OnBoard(100, 500);
      console.log(ferry);
      assert.equal();
      assert.equal();
     Done()
	});
});